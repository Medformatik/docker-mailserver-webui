name: docker_mailserver_webui
description: "A web UI that helps you manage your docker-mailserver configuration"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  openapi:
    path: ./openapi/

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
